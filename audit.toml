[advisories]
informational_warnings = ["unmaintained", "unsound", "notice"]
ignore = [
	"RUSTSEC-2026-0097", # Remaining rand 0.8 path is transitive via sanitizer/proc-macro deps; no safe upstream migration path fully removes it yet.
]
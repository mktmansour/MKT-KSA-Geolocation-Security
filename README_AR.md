# بسم الله الرحمن الرحيم

<div align="center">

# 🔒 MKT KSA Geolocation Security
### مكتبة أمان سيادية صفرية التبعيات للتطبيقات الحساسة

[![License](https://img.shields.io/badge/license-Apache--2.0-blue.svg)](LICENSE)
[![Rust](https://img.shields.io/badge/rust-1.70%2B-orange.svg)](https://www.rust-lang.org)
[![Zero Dependencies](https://img.shields.io/badge/dependencies-zero-success.svg)](Cargo.toml)
[![Security](https://img.shields.io/badge/security-sovereign-critical.svg)](docs/SBOM.md)

</div>

---

## 📋 جدول المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المستهدفون من المشروع](#المستهدفون-من-المشروع)
- [مميزات المشروع](#مميزات-المشروع)
- [المشاريع التي تحتاج هذه المكتبة](#المشاريع-التي-تحتاج-هذه-المكتبة)
- [البنية المعمارية](#البنية-المعمارية)
- [الميزات الرئيسية](#الميزات-الرئيسية)
- [التثبيت](#التثبيت)
- [بداية سريعة](#بداية-سريعة)
- [مسارات API](#مسارات-api)
- [حُرّاس الويب‑هوك](#حراس-الويبهوك)
- [تدوير المفاتيح](#تدوير-المفاتيح)
- [لوحة المعلومات](#لوحة-المعلومات)
- [التصدير والربط السحابي](#التصدير-والربط-السحابي)
- [واجهة FFI](#واجهة-ffi)
- [الميزات الاختيارية](#الميزات-الاختيارية)
- [أمثلة الاستخدام](#أمثلة-الاستخدام)
- [الاختبارات](#الاختبارات)
- [الأمان](#الأمان)
- [الترخيص](#الترخيص)
- [التواصل](#التواصل)

---

## 🎯 نظرة عامة

**MKT KSA Geolocation Security** هي مكتبة أمان سيادية متكاملة مُطوّرة بلغة Rust، مُصمّمة خصيصًا للتطبيقات الحساسة التي تتطلب **سيادة رقمية كاملة** و**صفر تبعيات خارجية**. 

توفر المكتبة نظام أمان متعدد الطبقات يشمل:
- تفتيش صارم للمدخلات والمخرجات
- بصمة سلامة رقمية
- تدوير مفاتيح تلقائي مرتبط بالمخاطر
- 50+ حارس ويب‑هوك لكل مسار مع توقيع HMAC-SHA512
- مانع إعادة (Anti-Replay) ذكي
- تليمترية تكيفية مدعومة بالذكاء الاصطناعي
- لوحة معلومات شاملة مع تعريب كامل
- واجهة FFI C-ABI للتكامل متعدد اللغات

### 🏆 لماذا هذه المكتبة؟

✅ **السيادة الرقمية**: صفر تبعيات خارجية — لا مكتبات طرف ثالث، لا مخاطر سلسلة التوريد  
✅ **الأمان المتعدد الطبقات**: حماية شاملة من الإدخال إلى الإخراج  
✅ **الذكاء التكيفي**: تشديد وارتخاء تلقائي حسب مستوى المخاطر  
✅ **الشفافية الكاملة**: مراقبة وتصدير جميع المؤشرات والأحداث  
✅ **المرونة والتوسع**: 50+ مسار API جاهز، سياسات قابلة للتحديث وقت التشغيل  
✅ **الأداء العالي**: نواة خفيفة ومحسّنة  

---

## 👥 المستهدفون من المشروع

### مطورو التطبيقات الحكومية والسيادية
الذين يحتاجون حلول أمان بلا تبعيات خارجية لضمان السيادة الرقمية الكاملة والامتثال للمعايير الوطنية.

### فرق الأمن السيبراني
لبناء أنظمة حماية متقدمة مع تفتيش صارم، بصمة سلامة، وكشف تسلل فوري.

### مطورو تطبيقات الخدمات المالية (FinTech)
الذين يحتاجون تدوير مفاتيح تلقائي، توقيع رقمي قوي، ومانع إعادة لحماية المعاملات المالية.

### شركات التقنية الصحية (HealthTech)
لحماية بيانات المرضى الحساسة مع تليمترية دقيقة وتتبع كامل للأحداث.

### منصات IoT والأجهزة الذكية
للتحقق من بصمة الجهاز، الموقع الجغرافي، والسلوك لمنع الانتحال والاختراق.

### مطورو Embedded Systems
الذين يحتاجون مكتبة خفيفة صفرية التبعيات مع واجهة FFI C-ABI للتكامل مع أنظمة مُدمجة.

### باحثو الأمن والأكاديميون
لدراسة وتطبيق آليات أمان متقدمة (Anti-Replay، HMAC، تدوير مفاتيح، سياسات DSL).

### فرق DevOps/SRE
لمراقبة الأمان عبر لوحة معلومات مركزية، تصدير متري، وربط سحابي لأنظمة SIEM.

---

## ⭐ مميزات المشروع

### 🛡️ السيادة الرقمية الكاملة
- **صفر تبعيات خارجية افتراضيًا** — لا مكتبات طرف ثالث، لا مخاطر سلسلة التوريد
- جميع الخوارزميات مُطبّقة داخليًا بدون استيراد خارجي
- تحكم كامل في الكود المصدري وقابل للتدقيق

### 🔐 أمان متعدد الطبقات

#### حُرّاس ويب‑هوك لكل مسار
- 50+ حارس مستقل مع توقيع HMAC-SHA512
- نوافذ زمنية قابلة للضبط لكل مسار
- تشديد تلقائي حسب حساسية المسار

#### مانع إعادة (Anti-Replay) قوي
- تتبع nonces لمنع إعادة تشغيل الطلبات
- مسح تكيفي (يومي/أسبوعي/شهري) حسب الحمل
- سعة وسلوك قابلين للضبط

#### تفتيش صارم
- فحص XSS، حقن SQL، ومسارات خطرة
- حدود أحجام للرؤوس والجسم
- تحقق UTF-8 وأنماط محتوى آمنة

#### حارس إخراج (Egress Guard)
- منع SSRF والوجهات الخطرة
- قوائم سماح/منع للمضيفين والمنافذ
- فحص RFC1918 وعناوين خاصة

### 🤖 ذكاء اصطناعي تكيفي

#### تشديد/ارتخاء تلقائي
- ضبط ديناميكي للأمان حسب مستوى المخاطر
- تقليص النوافذ الزمنية عند ارتفاع التهديدات
- ارتخاء آمن عند انخفاض المخاطر

#### تدوير مفاتيح ذاتي
- تدوير تلقائي عند تجاوز عتبة المخاطر
- دعم إصدارات متعددة (N و N+1)
- إشعارات ويب‑هوك عند التدوير

#### مسح مانع الإعادة الذكي
- جدولة تكيفية (يومي/أسبوعي/شهري)
- حساسية قابلة للضبط (0-100)
- إحصاءات وحالة مباشرة

### 📊 شفافية ومراقبة كاملة

#### لوحة معلومات شاملة
- HTML/JSON مع تعريب EN/AR
- مؤشرات مباشرة لكل العدّادات
- حالة تدوير المفاتيح ومانع الإعادة
- قائمة كاملة بالحُرّاس وإعداداتهم

#### تصدير متعدد الصيغ
- CSV متوافق مع Excel
- NDJSON للأحداث والمتري
- ربط سحابي لدفع البيانات

#### إحصاءات لكل مسار
- عدّادات التوقيع الناجح/الفاشل
- تتبع دقيق للأحداث
- سجل زمني كامل

### 🔧 مرونة وقابلية التوسع

#### سياسات حية
- تحديث JSON/DSL وقت التشغيل
- لا حاجة لإعادة تشغيل الخادم
- تطبيق فوري للسياسات الجديدة

#### 50+ مسار API جاهز
- إدارة مفاتيح كاملة
- نسخ احتياطي وجدولة
- جدار ناري ذكي
- تنبيهات ومراقبة

#### واجهة FFI C-ABI
- تكامل مع C/C++/Python/Java/.NET
- ترويسة مُولّدة تلقائيًا
- ABI ثابت عبر الإصدارات

### ⚡ أداء عالي وكفاءة

#### نواة خفيفة
- استهلاك ذاكرة ومعالج منخفض
- حارس ذاكرة تكيفي مع تفريغ تلقائي
- إدارة موارد محكمة

#### ضغط اختياري
- RLE للبيانات الكبيرة (>512B)
- عدّادات للضغط الوارد/الصادر
- تفعيل/تعطيل ديناميكي

#### جدار ناري ذكي
- قاطع دارة لحماية الموارد
- عدّادات مفصّلة (allowed/blocked)
- فتح/إغلاق يدوي للطوارئ

### ✅ جودة مضمونة

#### اختبارات شاملة
- Clippy بدون تحذيرات
- Miri للنواة الصفرية
- اختبارات تكامل كاملة
- fuzz-like لمدخلات عشوائية

#### توثيق كامل
- docs.rs شامل
- SBOM مفصّل
- أمثلة عملية

#### كود مفتوح
- Apache-2.0 License
- قابل للتدقيق بالكامل
- مساهمات مُرحب بها

### 🔑 تشفير سيادي

#### HMAC-SHA512 داخلي
- مُطبّق بدون تبعيات خارجية
- توقيع رقمي قوي
- تحقق آمن

#### مقارنات زمن ثابت
- ct_eq لمنع قنوات التوقيت
- حماية ضد هجمات التوقيت
- أمان مُثبت

#### تدوير مفاتيح متقدم
- دعم إصدارات متعددة
- انتقال سلس (N → N+1)
- عزل تدريجي للمفاتيح القديمة

---

## 🚀 المشاريع التي تحتاج هذه المكتبة

### 1. 🏛️ أنظمة الحكومة الإلكترونية
- بوابات الخدمات الحكومية السيادية
- أنظمة الهوية الرقمية والتحقق البيومتري
- منصات التصويت الإلكتروني الآمنة
- نظم إدارة المستندات الحكومية

### 2. 💰 الخدمات المالية والمصرفية
- تطبيقات المحافظ الرقمية والدفع الإلكتروني
- منصات التداول والعملات المشفرة
- أنظمة تحويلات مالية فورية
- بوابات دفع آمنة مع PCI DSS

### 3. 🏥 الرعاية الصحية
- أنظمة السجلات الطبية الإلكترونية (EMR/EHR)
- منصات التطبيب عن بعد (Telemedicine)
- تطبيقات تتبع اللقاحات والأوبئة
- أنظمة الوصفات الإلكترونية

### 4. 📡 إنترنت الأشياء (IoT)
- أنظمة المنزل الذكي مع تحقق بصمة الجهاز
- أجهزة تتبع المركبات والأصول
- شبكات الاستشعار الصناعية (IIoT)
- أنظمة المدن الذكية

### 5. 🛒 التجارة الإلكترونية
- منصات البيع عبر الإنترنت
- بوابات الدفع الآمنة
- أنظمة إدارة المخزون والتوريد
- برامج الولاء والمكافآت

### 6. 📚 التعليم والتعلم الإلكتروني
- منصات الامتحانات الإلكترونية
- أنظمة إدارة التعلم (LMS) الآمنة
- تطبيقات الشهادات الرقمية
- فصول افتراضية آمنة

### 7. 🚚 النقل والخدمات اللوجستية
- تطبيقات تتبع الشحنات مع تحقق جغرافي
- أنظمة إدارة الأساطيل
- منصات مشاركة الرحلات (Ride-sharing)
- حلول التوصيل الذكية

### 8. ⚡ الطاقة والمرافق
- أنظمة الشبكة الذكية (Smart Grid)
- مراقبة البنية التحتية الحرجة
- إدارة محطات الطاقة المتجددة
- عدادات ذكية آمنة

### 9. 🛡️ الأمن السيبراني
- أنظمة كشف التسلل (IDS/IPS)
- منصات SIEM (Security Information & Event Management)
- أدوات تحليل السلوك وكشف الشذوذ
- حلول SOC (Security Operations Center)

### 10. 📱 الاتصالات والإعلام
- منصات البث المباشر الآمنة
- تطبيقات المراسلة المشفرة
- أنظمة إدارة المحتوى (CMS) الحساسة
- خدمات VoIP آمنة

### 11. 🏭 الصناعة والتصنيع
- أنظمة التحكم الصناعي (SCADA/ICS)
- مراقبة خطوط الإنتاج
- إدارة الجودة الرقمية
- صيانة تنبؤية ذكية

### 12. 🔬 البحث العلمي
- منصات تبادل البيانات البحثية الحساسة
- أنظمة المختبرات الافتراضية
- قواعد بيانات الحمض النووي والمعلومات الحيوية
- شبكات التعاون البحثي الآمنة

---

## 🏗️ البنية المعمارية

```
┌─────────────────────────────────────────────────────────┐
│         Dashboard (HTML/JSON) + Exports (CSV)           │
│              EN/AR Localization Support                 │
├─────────────────────────────────────────────────────────┤
│           API Routes (50+ endpoints)                    │
│  /keys/* /backup/* /webhook/* /policy/* /memory/*      │
├─────────────────────────────────────────────────────────┤
│       Per-Path Webhook Guards (HMAC-SHA512)            │
│   Signature Verification + Anti-Replay + Timestamps    │
├─────────────────────────────────────────────────────────┤
│         Inspection Policy (JSON/DSL Runtime)            │
│    Method/Path/ContentType Allow/Deny + Limits         │
├─────────────────────────────────────────────────────────┤
│      Fingerprint (Integrity) + Telemetry (Events)      │
│         ct_eq comparisons + NDJSON logging             │
├─────────────────────────────────────────────────────────┤
│    Key Rotation + Anti-Replay Purge + Memory Guard     │
│      Adaptive AI-driven tightening/relaxation          │
├─────────────────────────────────────────────────────────┤
│   Egress Guard (SSRF Protection) + Smart Firewall      │
│    RFC1918 check + Circuit Breaker + Rate Limiting     │
├─────────────────────────────────────────────────────────┤
│         Core (Digest, Resolver, Analyzers)             │
│            Zero-Dependency Sovereign Core               │
└─────────────────────────────────────────────────────────┘
```

---

## 🎁 الميزات الرئيسية

### ✅ صفر تبعيات افتراضيًا
النواة الأساسية تعمل بدون أي مكتبات خارجية — سيادة رقمية كاملة.

### ✅ 50+ حارس ويب‑هوك مستقل
كل مسار API محمي بحارس خاص قابل للتهيئة (توقيع، نافذة زمنية، مانع إعادة).

### ✅ تدوير مفاتيح تلقائي
مرتبط بمستوى المخاطر — تدوير ذاتي عند تجاوز العتبة المحددة.

### ✅ مانع إعادة تكيفي
مسح دوري ذكي (يومي/أسبوعي/شهري) حسب الحمل والحساسية.

### ✅ تليمترية ذكية
تشديد وارتخاء تلقائي للحُرّاس حسب المخاطر وأخطاء التوقيع.

### ✅ لوحة معلومات شاملة
HTML/JSON مع تعريب EN/AR كامل، عرض مؤشرات وحُرّاس وحالة.

### ✅ تصدير CSV/NDJSON
متوافق مع Excel، تصدير جميع المؤشرات والأحداث.

### ✅ ربط سحابي اختياري
دفع المقاييس لأنظمة خارجية (SIEM، مراقبة، تحليلات).

### ✅ سياسات حية
تحديث JSON/DSL وقت التشغيل بدون إعادة تشغيل.

### ✅ حارس إخراج
حماية ضد SSRF، فحص RFC1918، قوائم سماح/منع.

### ✅ حارس ذاكرة
تفريغ تلقائي/يدوي للأحداث عند تجاوز الحد، مع تنبيهات.

### ✅ واجهة FFI C-ABI
تكامل مع C/C++/Python/Java/.NET، ترويسة مُولّدة، ABI ثابت.

### ✅ ضغط RLE اختياري
للبيانات الكبيرة (>512B)، تفعيل/تعطيل ديناميكي.

### ✅ جدار ناري ذكي
قاطع دارة، عدّادات مفصّلة، فتح/إغلاق يدوي.

### ✅ توقيع HMAC-SHA512
مُطبّق داخليًا بدون تبعيات، مقارنات ct_eq آمنة.

---

## 📦 التثبيت

### من Crates.io

```toml
[dependencies]
mkt_ksa_geo_sec = "1.0.2"
```

### من المصدر

```bash
git clone https://github.com/mktmansour/MKT-KSA-Geolocation-Security
cd MKT-KSA-Geolocation-Security
cargo build --release
```

### البناء مع ميزات محددة

```bash
# خادم HTTP فقط
cargo build --features api_std_http

# خادم + HMAC
cargo build --features "api_std_http,sign_hmac"

# خادم + HMAC + RLE + FFI
cargo build --features "api_std_http,sign_hmac,compress_rle,ffi_c"
```

---

## 🚀 بداية سريعة

### مثال بسيط

```rust
use mkt_ksa_geo_sec::api::std_http;
use mkt_ksa_geo_sec::security::inspection_policy::InboundPolicy;
use std::sync::Arc;

fn main() {
    // تهيئة التليمترية
    mkt_ksa_geo_sec::telemetry::init();
    
    // سياسة افتراضية آمنة
    let policy = InboundPolicy::default();
    
    // معالج بسيط
    let handler = Arc::new(|req: &std_http::Request| {
        std_http::Response::json(200, r#"{"status":"ok","endpoints":["/dashboard","/metrics"]}"#)
    });
    
    // تشغيل الخادم
    println!("الخادم يعمل على http://127.0.0.1:8080");
    std_http::run_with_policy("127.0.0.1:8080", policy, handler).unwrap();
}
```

### مثال مع ويب‑هوك

```rust
use mkt_ksa_geo_sec::{api::std_http, webhook, telemetry};
use std::sync::Arc;

struct MyWebhook;

impl webhook::WebhookEndpoint for MyWebhook {
    fn receive(&self, payload: &str) -> Result<(), webhook::WebhookError> {
        telemetry::record_event("webhook_received", payload);
        
        // فحص محتوى خطر
        if payload.contains("attack") {
            telemetry::set_risk(80);
            return Err(webhook::WebhookError::InvalidPayload);
        }
        
        Ok(())
    }
}

fn main() {
    telemetry::init();
    
    // ربط ويب‑هوك
    std_http::set_webhook_endpoint(Arc::new(MyWebhook));
    
    // تشغيل مع سياسة
    let policy = webhook::SecurityPolicy::strict();
    let handler = Arc::new(|req| {
        std_http::Response::json(200, r#"{"ok":true}"#)
    });
    
    std_http::run_with_policy("127.0.0.1:8080", policy, handler).unwrap();
}
```

---

## 🔌 مسارات API

### 📨 ويب‑هوك (Webhooks) — استقبال خارجي

| المسار | الطريقة | الوصف | الحارس |
|--------|---------|-------|--------|
| `/ai/ingest` | POST | استقبال بيانات ذكاء اصطناعي | required, auth_hmac |
| `/ai/model/update` | POST | تحديث نموذج AI | required, auth_hmac |
| `/ai/feedback` | POST | تغذية راجعة للنماذج | required, auth_hmac |
| `/weather/hook` | POST | بيانات طقس خارجية | required, weather_hmac |
| `/weather/alerts` | POST | تنبيهات طقس سياقية | required, weather_hmac |
| `/alerts/in` | POST | تنبيهات أمنية خارجية | required, auth_hmac |
| `/partner/events` | POST | أحداث شريك خارجي | required, partner_hmac |
| `/partner/telemetry` | POST | تليمترية شريك | required, partner_hmac |
| `/geo/satellite` | POST | بيانات أقمار صناعية | required, auth_hmac |
| `/geo/maplayer` | POST | طبقات خرائط | required, auth_hmac |
| `/webhook/in` | POST | استقبال عام | required, auth_hmac |

### 🔑 إدارة المفاتيح (Key Management)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/keys/create` | POST | إنشاء مفتاح جديد |
| `/keys/rotate` | POST | تدوير مفتاح قائم |
| `/keys/meta` | GET | جلب ميتاداتا المفاتيح |
| `/keys/export_hex` | GET | تصدير مفاتيح Hex (يحتاج consent) |
| `/keys/auto/config` | POST | تهيئة تدوير تلقائي |
| `/keys/auto/disable` | POST | تعطيل تدوير تلقائي |

### 💾 نسخ احتياطي (Backup)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/backup/download` | GET | تنزيل سجل الأحداث NDJSON |
| `/backup/send` | POST | إرسال نسخة لوجهة خارجية |
| `/backup/consent` | POST | تعيين رمز موافقة |
| `/backup/schedule` | POST | جدولة نسخ دوري |
| `/backup/schedule/disable` | POST | إلغاء الجدولة |
| `/backup/email` | POST | إرسال عبر البريد (ميزة smtp_std) |

### 🧱 جدار ناري (Firewall)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/fw/metrics` | GET | مؤشرات الجدار |
| `/fw/open` | POST | فتح قاطع الدارة (حجب وقائي 503) |
| `/fw/close` | POST | إغلاق القاطع (استعادة الخدمة) |

### 📊 لوحة المعلومات والتصدير

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/dashboard` | GET | لوحة HTML/JSON (lang=en\|ar, fmt=html) |
| `/metrics` | GET | مؤشرات JSON مباشرة |
| `/events.ndjson` | GET | بث الأحداث NDJSON |
| `/export/csv` | GET | تصدير CSV (type=metrics\|events) |
| `/cloud/push` | POST | دفع للسحابة (url=...) |

### 🛡️ حُرّاس الويب‑هوك

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/webhook/guard/list` | GET | قائمة الحُرّاس |
| `/webhook/guard/set` | POST | ضبط حارس لمسار |
| `/webhook/guard/disable` | POST | تعطيل حارس |
| `/webhook/guard/stats` | GET | إحصاءات التوقيع |

### 📜 السياسات (Policies)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/policy/get` | GET | عرض السياسة الحية |
| `/policy/set` | POST | تطبيق JSON وقت التشغيل |
| `/policy/set_dsl` | POST | تطبيق DSL نصّي |

### 🔄 مانع الإعادة (Anti-Replay Purge)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/anti_replay/purge/config` | POST | تهيئة المسح (mode/sensitivity/window/capacity) |
| `/anti_replay/purge/disable` | POST | تعطيل المسح |
| `/anti_replay/purge/run` | POST | تشغيل فوري |
| `/anti_replay/purge/status` | GET | حالة المسح |

### 🧠 الذاكرة (Memory Guard)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/memory/config` | POST | ضبط حد وتفعيل (limit/auto) |
| `/memory/purge` | POST | تفريغ فوري |
| `/memory/status` | GET | حالة الذاكرة |

### 🚨 التنبيهات (Alerts)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/alerts/set` | POST | ضبط تنبيه مخاطر |
| `/alerts/disable` | POST | تعطيل التنبيهات |

### 🎨 أخرى

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/templates/set` | POST | ضبط قوالب بريد |
| `/templates/default` | POST | تعيين قالب افتراضي |
| `/toggle` | GET | تبديل ضغط (compression=on\|off) |
| `/features/enable` | POST | تفعيل ميزة (name=ai_insights\|cloud\|csv_export) |
| `/features/disable` | POST | تعطيل ميزة |
| `/lang/set` | POST | تعيين لغة (lang=en\|ar) |

---

## 🛡️ حُرّاس الويب‑هوك

### ما هي الحُرّاس؟

الحُرّاس هي آلية أمان لكل مسار توفر:
- **توقيع HMAC-SHA512** لضمان سلامة المصدر
- **مانع إعادة** لمنع تكرار الطلبات
- **نافذة زمنية** محدودة لقبول الطلبات
- **تشديد/ارتخاء تكيفي** حسب المخاطر

### إعدادات الحارس

```rust
pub struct GuardConfig {
    pub path: String,           // المسار
    pub alg: String,            // "hmac-sha512" أو "none"
    pub key_id: String,         // معرّف المفتاح
    pub required: bool,         // إلزام التوقيع؟
    pub ts_window_ms: u64,      // نافذة زمنية (ميلي ثانية)
    pub anti_replay_on: bool,   // تفعيل مانع الإعادة؟
}
```

### مثال: ضبط حارس

```bash
# ضبط حارس لمسار /ai/ingest
curl -X POST "http://127.0.0.1:8080/webhook/guard/set?path=/ai/ingest&alg=hmac-sha512&key=auth_hmac&ts=300000&required=1&replay=1"

# عرض جميع الحُرّاس
curl http://127.0.0.1:8080/webhook/guard/list

# إحصاءات التوقيع
curl http://127.0.0.1:8080/webhook/guard/stats
```

### مثال: إرسال طلب موقّع

```python
import hmac
import hashlib
import time
import requests

def sign_request(method, path, body, key):
    ts = str(int(time.time() * 1000))
    nonce = hashlib.sha256(ts.encode()).hexdigest()[:16]
    
    # النص الموقّع: method|path|content-type|timestamp|nonce|sha512(body)
    body_hash = hashlib.sha512(body.encode()).hexdigest()
    canonical = f"{method}|{path}|application/json|{ts}|{nonce}|{body_hash}"
    
    # حساب HMAC-SHA512
    sig = hmac.new(key.encode(), canonical.encode(), hashlib.sha512).hexdigest()
    
    return {
        'X-MKT-Alg': 'hmac-sha512',
        'X-MKT-KeyId': 'auth_hmac',
        'X-MKT-Timestamp': ts,
        'X-MKT-Nonce': nonce,
        'X-MKT-Signature': sig
    }

# إرسال طلب موقّع
headers = sign_request('POST', '/ai/ingest', '{"data":"test"}', 'your-secret-key')
headers['Content-Type'] = 'application/json'
r = requests.post('http://127.0.0.1:8080/ai/ingest', 
                  json={"data":"test"}, 
                  headers=headers)
print(r.json())
```

### التشديد التلقائي

عند ارتفاع أخطاء التوقيع أو المخاطر، تُشدَّد الحُرّاس تلقائيًا:
- تقليص النافذة الزمنية `ts_window_ms`
- فرض التوقيع `required=true`
- تفعيل مانع الإعادة `anti_replay_on=true`

### الارتخاء الآمن

عند انخفاض المخاطر وقلة الأخطاء، تُرخى الحُرّاس نحو إعداداتها الأساسية.

---

## 🔑 تدوير المفاتيح

### التدوير اليدوي

```bash
# إنشاء مفتاح
curl -X POST "http://127.0.0.1:8080/keys/create?id=auth_hmac&ver=1&len=32"

# تدوير المفتاح
curl -X POST "http://127.0.0.1:8080/keys/rotate?id=auth_hmac&ver=2&len=32"

# جلب ميتاداتا
curl "http://127.0.0.1:8080/keys/meta?id=auth_hmac"
```

### التدوير التلقائي

```bash
# تهيئة تدوير تلقائي
# threshold: عتبة المخاطر للتدوير (0-100)
# interval: فترة فحص بالثواني
# ids: قائمة معرّفات المفاتيح
# len: طول المفتاح بالبايتات
curl -X POST "http://127.0.0.1:8080/keys/auto/config?threshold=85&interval=300&ids=auth_hmac,backup_key&len=32"

# تعطيل التدوير التلقائي
curl -X POST "http://127.0.0.1:8080/keys/auto/disable"
```

### دعم إصدارات متعددة

يدعم النظام قبول إصدارين من المفتاح في آن واحد (N و N+1) لضمان انتقال سلس دون انقطاع الخدمة:

```
قبل التدوير:   v1 (نشط)
أثناء التدوير: v1 (نشط)  +  v2 (نشط)
بعد التدوير:   v2 (نشط),  v1 (معطّل تدريجيًا)
```

---

## 📊 لوحة المعلومات

### الوصول

```bash
# HTML باللغة العربية
http://127.0.0.1:8080/dashboard?fmt=html&lang=ar

# HTML بالإنجليزية
http://127.0.0.1:8080/dashboard?fmt=html&lang=en

# JSON
http://127.0.0.1:8080/dashboard
```

### ما تعرضه اللوحة

#### 📈 المؤشرات المباشرة
- عدد الطلبات المفحوصة/المحجوبة
- نجاح/فشل التوقيع
- ضغط وارد/صادر
- بصمات مُحسوبة
- ويب‑هوك وارد/صادر

#### 🔑 حالة تدوير المفاتيح
- مُفعّل/معطّل
- عتبة المخاطر
- فترة الفحص
- آخر تدوير
- عدد المفاتيح المُتتبّعة

#### 🔄 حالة مسح Anti-Replay
- مُفعّل/معطّل
- النمط (يومي/أسبوعي/شهري)
- فترة المسح
- المسح التالي
- الحساسية (0-100)

#### 🧠 حالة الذاكرة
- الحد المُحدّد
- تفريغ تلقائي؟
- الأحداث المُستخدمة
- تحذيرات التجاوز

#### 🎛️ الميزات المُفعّلة
- AI Insights
- Cloud Integration
- CSV Export

#### 🛡️ قائمة الحُرّاس
- المسار
- الخوارزمية
- معرّف المفتاح
- إلزامي؟
- النافذة الزمنية
- مانع الإعادة؟

#### 📊 إحصاءات التوقيع
- لكل مسار: نجاح/فشل
- إجمالي التوقيعات
- معدل النجاح

---

## 📤 التصدير والربط السحابي

### تصدير CSV

```bash
# تصدير المؤشرات
curl "http://127.0.0.1:8080/export/csv?type=metrics" -o metrics.csv

# تصدير الأحداث
curl "http://127.0.0.1:8080/export/csv?type=events" -o events.csv
```

### تصدير NDJSON

```bash
# تنزيل سجل الأحداث
curl "http://127.0.0.1:8080/backup/download" -o events.ndjson

# بث الأحداث مباشرة
curl "http://127.0.0.1:8080/events.ndjson"
```

### دفع للسحابة

```bash
# تفعيل ميزة السحابة أولاً
curl -X POST "http://127.0.0.1:8080/features/enable?name=cloud"

# دفع المقاييس
curl -X POST "http://127.0.0.1:8080/cloud/push?url=https://api.example.com/receive"
```

---

## 🔗 واجهة FFI

### البناء مع FFI

```bash
cargo build --release --features ffi_c

# توليد الترويسة
# يُولّد تلقائيًا في: include/mkt_ksa_geo_sec.h
```

### الترويسة المُولّدة

```c
// include/mkt_ksa_geo_sec.h

// إصدار ABI (ثابت)
uint32_t mkt_abi_version(void);

// نسخة SemVer (NUL-terminated)
const char* mkt_semver_string(void);

// HMAC-SHA512
int32_t mkt_hmac_sha512(
    const uint8_t* data_ptr,
    size_t data_len,
    const uint8_t* key_ptr,
    size_t key_len,
    uint8_t* out_ptr,
    size_t out_len
);
```

### مثال C

```c
#include "mkt_ksa_geo_sec.h"
#include <stdio.h>

int main() {
    // عرض الإصدار
    printf("ABI: %u\n", mkt_abi_version());
    printf("Version: %s\n", mkt_semver_string());
    
    // حساب HMAC
    const uint8_t data[] = "Hello, World!";
    const uint8_t key[] = "secret-key";
    uint8_t out[64];
    
    int ret = mkt_hmac_sha512(data, 13, key, 10, out, 64);
    if (ret == 0) {
        printf("HMAC: ");
        for (int i = 0; i < 64; i++) {
            printf("%02x", out[i]);
        }
        printf("\n");
    }
    
    return 0;
}
```

### الربط

```bash
# Linux/macOS
gcc -o example example.c -L./target/release -lmkt_ksa_geo_sec

# Windows
cl.exe example.c mkt_ksa_geo_sec.lib
```

---

## 🎛️ الميزات الاختيارية

| الميزة | الوصف | التبعيات |
|--------|-------|---------|
| `api_std_http` | خادم HTTP/1.1 داخلي صفري | ✅ صفر |
| `sign_hmac` | توقيع HMAC-SHA512 داخلي | ✅ صفر |
| `compress_rle` | ضغط/فك ضغط RLE | ✅ صفر |
| `egress` | طبقة حارس الإخراج | ✅ صفر |
| `egress_http_std` | عميل HTTP خروج بسيط | ✅ صفر |
| `smtp_std` | إرسال بريد بسيط (TCP) | ✅ صفر |
| `ffi_c` | واجهة C-ABI + ترويسة | ✅ صفر |
| `ledger_blake3` | سجل أحداث BLAKE3 | ✅ صفر |
| `core_utils` | أدوات نواة مساعدة | ✅ صفر |
| `input_validation` | تحقق إدخال إضافي | ✅ صفر |

**ملاحظة**: جميع الميزات صفرية التبعيات — لا تُضيف مكتبات خارجية.

---

## 💡 أمثلة الاستخدام

### تشغيل Demo Dashboard

```bash
# تعيين متغيرات البيئة (Windows)
$env:CARGO_HOME="C:\rust\cargo"
$env:RUSTUP_HOME="C:\rust\rustup"

# تشغيل
cargo run --bin std_dashboard_demo --features api_std_http

# الوصول
# http://127.0.0.1:8080/dashboard?fmt=html&lang=ar
```

### تفعيل ميزة AI

```bash
curl -X POST "http://127.0.0.1:8080/features/enable?name=ai_insights"
```

### ضبط حد الذاكرة

```bash
# حد 1MB مع تفريغ تلقائي
curl -X POST "http://127.0.0.1:8080/memory/config?limit=1048576&auto=1"

# تفريغ فوري
curl -X POST "http://127.0.0.1:8080/memory/purge"

# حالة
curl "http://127.0.0.1:8080/memory/status"
```

### تهيئة مسح Anti-Replay

```bash
# مسح يومي بحساسية 70
curl -X POST "http://127.0.0.1:8080/anti_replay/purge/config?mode=daily&sensitivity=70&window=300000&capacity=2048"

# تشغيل فوري
curl -X POST "http://127.0.0.1:8080/anti_replay/purge/run"

# حالة
curl "http://127.0.0.1:8080/anti_replay/purge/status"
```

### تطبيق سياسة DSL

```bash
# إنشاء ملف policy.dsl
cat > policy.dsl << 'EOF'
# السياسة الأمنية
allowed_methods=GET,POST
allowed_path_prefixes=/api,/webhook
denied_path_prefixes=/admin,/internal
allowed_content_types=application/json,text/plain
limits.max_headers_bytes=8192
limits.max_body_bytes=1048576
EOF

# تطبيق
curl -X POST "http://127.0.0.1:8080/policy/set_dsl" \
     -H "Content-Type: text/plain" \
     --data-binary @policy.dsl
```

---

## 🧪 الاختبارات

### اختبارات النواة (صفر تبعيات)

```bash
# اختبارات المكتبة
cargo test --no-default-features

# Clippy صارم
cargo clippy --lib --no-default-features -- -D warnings

# التنسيق
cargo fmt --all

# Miri (فحص أمان الذاكرة)
cargo +nightly miri test --no-default-features
```

### اختبارات الخادم

```bash
# اختبارات مع api_std_http
cargo test --features api_std_http

# Clippy للثنائية
cargo clippy --bins --features api_std_http -- -D warnings
```

### اختبار شبه-Fuzz

```bash
# اختبار مدخلات عشوائية
cargo test --test fuzz_like --features api_std_http
```

---

## 🔐 الأمان

### سياسة الإبلاغ عن الثغرات

إذا اكتشفت ثغرة أمنية، يُرجى إرسال تقرير خاص إلى:
**mkt-edge@outlook.sa**

يُرجى **عدم** نشر الثغرات في Issues العامة.

### أفضل الممارسات المُطبّقة

✅ مقارنات زمن ثابت (ct_eq) للبصمات والتواقيع  
✅ حدود صارمة للمدخلات (رؤوس/جسم)  
✅ تحقق UTF-8 وفحص XSS  
✅ حارس إخراج ضد SSRF  
✅ مانع إعادة قوي  
✅ توقيع HMAC-SHA512 داخلي  
✅ تشديد تكيفي حسب المخاطر  
✅ تدقيق مستمر (Clippy/Miri/Tests)  

### التدقيق الخارجي

المشروع مفتوح المصدر وقابل للتدقيق بالكامل. نُرحب بالمراجعات الأمنية من المجتمع.

---

## 📄 الترخيص

هذا المشروع مُرخّص تحت **Apache License 2.0**.

راجع ملف [LICENSE](LICENSE) للتفاصيل الكاملة.

---

## 📞 التواصل

### المؤلف
**Mansour Khalid**  
📧 mkt-edge@outlook.sa

### الروابط

- 🌐 **Repository**: [github.com/mktmansour/MKT-KSA-Geolocation-Security](https://github.com/mktmansour/MKT-KSA-Geolocation-Security)
- 📚 **Documentation**: [docs.rs/MKT_KSA_Geolocation_Security](https://docs.rs/MKT_KSA_Geolocation_Security)
- 📦 **Crates.io**: [crates.io/crates/MKT_KSA_Geolocation_Security](https://crates.io/crates/MKT_KSA_Geolocation_Security)
- 🐛 **Issues**: [GitHub Issues](https://github.com/mktmansour/MKT-KSA-Geolocation-Security/issues)

---

## 🙏 شكر وتقدير

شكرًا لمجتمع Rust على الأدوات الممتازة والدعم المستمر.

---

## 📚 موارد إضافية

- [docs/SBOM.md](docs/SBOM.md) - قائمة مكوّنات البرمجيات
- [docs/Final_Engineering_Report.md](docs/Final_Engineering_Report.md) - التقرير الهندسي الشامل
- [docs/Test_Plan.md](docs/Test_Plan.md) - خطة الاختبار
- [include/mkt_ksa_geo_sec.h](include/mkt_ksa_geo_sec.h) - ترويسة FFI C-ABI

---

<div align="center">

**صُنع بـ ❤️ في المملكة العربية السعودية**

</div>

